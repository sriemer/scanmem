language: c
sudo: required

services:
  - docker

env:
  global:
  # The next declaration is the encrypted COVERITY_SCAN_TOKEN, created
  #   via the "travis encrypt" command using the project repo's public key
  - secure: "C3Gd8W9U0yQIxnUkcKlzicB2iOeZ42NPpVXTOnigjoc2UoI4eVYlDjEZjccZAIn0wHUguircvBt0eyLD7cuNf2mTozJ21BG0NpMdYbG1n/aVchnJBr6rldb2X3kVmQCj50LQKm+aINbK1qSs56VIUwNepPiul+HPMV6sL5sQ5M0="
  - ASAN_OPTIONS='halt_on_error=1'
  - UBSAN_OPTIONS='halt_on_error=1'

before_install:
  - docker pull ubuntu:bionic
  - docker run -t -d --name="bionic" ubuntu:bionic /bin/bash
  - docker ps
  - docker exec bionic /bin/sh -c "apt-get update && apt-get upgrade -y"
  - docker exec bionic /bin/sh -c "apt-get install -y git gcc binutils libtool intltool make libreadline-dev python"
  - echo -n | openssl s_client -connect scan.coverity.com:443 | sed -ne '/-BEGIN CERTIFICATE-/,/-END CERTIFICATE-/p' | sudo tee -a /etc/ssl/certs/ca-

addons:
  apt:
    packages:
    - gcc binutils intltool
  # Coverity scan add-on, fires only when pushing to the `coverity_scan` branch
  coverity_scan:
    project:
      name: "scanmem/scanmem"
      description: "Build submitted via Travis CI"
    notification_email: andreastacchiotti@gmail.com
    build_command_prepend: "./autogen.sh && ./configure"
    build_command: "make"
    branch_pattern: coverity_scan

script:
  - if [ "${COVERITY_SCAN_BRANCH}" == 1 ]; then exit ; fi
  - docker exec bionic /bin/bash -c "gcc --version"
  - docker exec bionic /bin/bash -c "git clone --depth=50 --branch=${TRAVIS_BRANCH} https://github.com/${PROJECT_NAME} && cd scanmem && git branch && git show --oneline HEAD | head -n1"
  - docker exec bionic /bin/bash -c "cd scanmem && ./autogen.sh && ./configure --enable-gui"
  - docker exec bionic /bin/bash -c "cd scanmem && make CFLAGS='-O2 -fsanitize=address,undefined'"
  - docker exec bionic /bin/bash -c "cd scanmem && make check VERBOSE=1"
